#!/usr/bin/env bash
# Generated by install.sh — runs whisper-desk in its isolated environment.
set -euo pipefail

APP_DIR="@APP_DIR@"
VENV="@VENV@"

export PYTHONPATH="$APP_DIR/src${PYTHONPATH:+:$PYTHONPATH}"
# The GTK overlay runs with the system Python: it is the one with PyGObject.
export WD_SYSTEM_PYTHON="${WD_SYSTEM_PYTHON:-@SYSTEM_PYTHON@}"

# CUDA libraries bundled in the venv (cuBLAS/cuDNN for CTranslate2).
for dir in "$VENV"/lib/python*/site-packages/nvidia/*/lib; do
    [ -d "$dir" ] && LD_LIBRARY_PATH="$dir${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
done
export LD_LIBRARY_PATH="${LD_LIBRARY_PATH:-}"

exec "$VENV/bin/python" -m whisper_desk "$@"
